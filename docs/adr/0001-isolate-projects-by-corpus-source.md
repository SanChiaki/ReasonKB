# Isolate projects by corpus source

ReasonKB will keep projects discovered from different Corpus Sources distinct, even when their names match. Each Project belongs to exactly one Corpus Source; ReasonKB will not merge projects by name or aggregate multiple sources into one project. This prevents accidental content mixing and lets each source manage synchronization and deletion independently, while conversations can still select multiple projects when cross-project retrieval is needed.
