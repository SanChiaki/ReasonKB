# Manage corpus sources at deployment scope

Corpus Sources and their credentials are owned by the ReasonKB deployment and managed by Source Administrators. They are not owned by individual ReasonKB users, and this multi-source change does not introduce per-user source connections or source sharing. This matches the application's deployment-wide settings; Projects produced by those sources are shared across the deployment under the authorization boundary recorded separately.
